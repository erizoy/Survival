R6:

1. Реализованы компоненты меню Вид
2. Добавлена проверка на одинаковые имена категорий
3. Изменено отображение даты на главной форме (с полного формата даты со временем на формат только даты)
4. Немного изменен внешний вид формы BuyingForm

R7:

1. Добавлена категория "Приход". Она является полностью изолированной от категорий расхода и недоступна для изменения пользователем.
2. Добавлены комментарии к совершенным операциям. Комментарии отображаются в всплывающем окне, при наведении курсора на ячейку "Описание".
3. Теперь категории расхода подсвечиваются красным. Приход - зеленым.
4. добавлены обработчики кнопок (Enter, Esc) на все формы, кроме главной.
5. Меню Вид: добавлен единичный выбор категории по двойному клику, оптимизорована работа фильтра по категориям, исправлены мелкие баги.
6. Добавлена стандартная сортировка столбцов по дате (убывание).
7. Добавлена проверка целостности данных для совершаемых операций.

R8:

1. CategoryFrom.cs переименован в CategoryForm.cs
2. Оптимизирована проверка на существование категории с таким же именем в CategoryForm.cs
3. Теперь элемент Item хранит не Name категории, а индекс категории в списке категорий. Это необходимо. 
4. Категории можно удалять клавишей Delete
5. Проверка целостности данных при удалении категорий: при попытке удалить категорию, в которой есть продукты - появляется предупреждение. Если пользователь подтверждает удаление - то этим продуктам присвается неопределенная категория с индексом равным -1.
6. При редактировании и добавлении покупок если не выбрана категория то появляется вопрос с подтверждением, при подтверждении - категория устанавливается в -1.
7. У доходов категория равна константе Incom.INDEX. При раскраске списка сравнивается не имя категории со строкой Income.Name, а индекс категории с константой.
8. Везде в формах убрана переменная ok, которая показывает, перерисовывать содержимое вызывающей формы или нет. Вместо ok используется стандартный DialogResult.
9. В Buyings добавлены свойства TotalIncome и TotalOutcome (не реализованы)


r9: test release. One space in one class. No changes.

r10:

1. Реализован общий фильтр. Функционал полностью повторяет бывшее меню "Вид". За не надобностью оно было удалено. Помним... Скорбим... R.I.P.
2. В полях "Сумма" отключена возможность вводить неправильные символы.
3. Метод dataGridView1CellDoubleClick() был приведен в исходное состояние. Раньше считывались индексы таблицы, неважно как она была отсортирована (если 1ый эелемент операций оказался в гриде 4ым, и мы нажмем по нему двойным кликом, то появлялась форма для 4ого элемента операций).
4. Добавлено окно о программе.

r11:
1. Повыделывался с дизайном
2. Реализовал TotalIncome и TotalOutcome свойства в Buyings
3. Переименовал наконец Приход в Доход

r12
1. Вывод в xls файл 
2. для этого добавлены reference's на мс офис и созданы доп методы обработки в formmain
3. не корректо выводятся даты, но похер..не знаю тчо как надо спросить

r13
1. ничего нового, жалкие попытки сделать диаграмму

r20
цвета в категориях, диаграмма без подписей, но разными цветами
прога теперь загружается без файла serialize.xml, но теперь нет фильтров
дополнительно смотрите "to do.txt"

r22
закончен фильтр, категории, итемы
начата документация

r23
исправлена ошибка с выводом в ексель, при нажатии на кнопку отмена. Исправленны добавленные ранее разработчиками косяки в виде удаления важных референсов и прочих.